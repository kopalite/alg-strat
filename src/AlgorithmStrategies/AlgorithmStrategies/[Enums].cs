namespace AlgorithmStrategies
{
    public enum ResultType
    {
        Inconclusive = 0,
        Success,
        Failure
    }

    public enum TerminationType
    {
        Never = 0,
        OnSuccess,
        OnFailure,
        Always
    }
}
